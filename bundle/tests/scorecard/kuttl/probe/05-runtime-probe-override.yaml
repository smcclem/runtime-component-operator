apiVersion: rc.app.stacks/v1beta2
kind: RuntimeComponent
metadata:
  name: probes-rc
spec:
  # Add fields here
  service:
    port: 9443
  replicas: 1
  probes:
    startup:
      initialDelaySeconds: 3
    readiness:
    liveness:
